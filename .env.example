# ── Convex ──
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
CONVEX_DEPLOY_KEY=prod:your-deploy-key

# ── AI (add one or both — app works without either via fallback responses) ──
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# ── Avatar APIs (Phase 2 — not needed for MVP) ──
# HEYGEN_API_KEY=
# DID_API_KEY=

# ── Voice (Phase 2 — MVP uses free Web Speech API) ──
# ELEVENLABS_API_KEY=

# ── Video Sessions (Daily.co — app works in demo mode without a key) ──
# DAILY_API_KEY=

# ── App ──
NEXT_PUBLIC_APP_URL=http://localhost:3000
