# ── Convex ──
NEXT_PUBLIC_CONVEX_URL=https://your-project.convex.cloud
CONVEX_DEPLOY_KEY=prod:your-deploy-key

# ── AI (add one or both — app works without either via fallback responses) ──
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...

# ── PostHog Product Analytics ──
NEXT_PUBLIC_POSTHOG_KEY=phc_your-posthog-project-key
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com  # EU Cloud — change to us.i.posthog.com for US region

# ── Google Analytics 4 ──
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ── Sentry ──
NEXT_PUBLIC_SENTRY_DSN=https://xxx@xxx.ingest.us.sentry.io/xxx
SENTRY_AUTH_TOKEN=sntrys_...
SENTRY_ORG=your-sentry-org
SENTRY_PROJECT=ratio

# ── Avatar APIs (Phase 2 — not needed for MVP) ──
# HEYGEN_API_KEY=
# DID_API_KEY=

# ── Voice (ElevenLabs — British "Daniel" voice for AI Judge TTS) ──
# Four-tier TTS fallback: ElevenLabs → Edge TTS → Browser SpeechSynthesis → Silent
# App works without this key — browser voice kicks in as fallback.
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ── Video Sessions (Daily.co — app works in demo mode without a key) ──
# DAILY_API_KEY=

# ── Stripe (optional — app works without Stripe in free-tier mode) ──
# STRIPE_SECRET_KEY=sk_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PREMIUM_PRICE_ID=price_...
# STRIPE_PREMIUM_PLUS_PRICE_ID=price_...

# ── Demo Credentials (testing) ──
# Set to "true" to show demo credentials banner on auth pages.
# Auto-detected on localhost / Vercel preview if not set.
# NEXT_PUBLIC_SHOW_DEMO_CREDENTIALS=true
# NEXT_PUBLIC_DEMO_EMAIL=demo@ratio.law
# NEXT_PUBLIC_DEMO_PASSWORD=DemoAdvocate2026

# ── Discord Notifications (optional — sends real-time event notifications to a Discord channel) ──
# Create a webhook in Discord: Server Settings > Integrations > Webhooks > New Webhook
# Set this as a Convex environment variable: npx convex env set DISCORD_WEBHOOK_URL "https://discord.com/api/webhooks/..."
# If not set, Discord notifications are silently skipped.
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# ── Email Notifications (Resend — optional, sends welcome emails and admin alerts) ──
# Sign up at resend.com to get an API key. If not set, email sending is silently skipped.
# Set these as Convex environment variables:
#   npx convex env set RESEND_API_KEY "re_..."
#   npx convex env set ADMIN_EMAIL "you@example.com"
#   npx convex env set RESEND_FROM_EMAIL "Ratio <notifications@yourdomain.com>"
# RESEND_API_KEY=re_...
# ADMIN_EMAIL=your-email@example.com
# RESEND_FROM_EMAIL=Ratio <onboarding@resend.dev>

# ── App ──
NEXT_PUBLIC_APP_URL=https://ratiothedigitalcourtsociety.com
